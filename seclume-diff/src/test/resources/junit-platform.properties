# Test classes side by side, four at a time; the methods of one class one
# after the other. Every class here works on tables of its own, and the
# differential classes spend their time waiting for a database - the module
# was the last on the build's critical path at 174 s serial.
# Classes that change something global or measure time carry @Isolated.
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=same_thread
junit.jupiter.execution.parallel.mode.classes.default=concurrent
junit.jupiter.execution.parallel.config.strategy=fixed
junit.jupiter.execution.parallel.config.fixed.parallelism=4
